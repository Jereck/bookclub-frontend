import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "example.com", "images.unsplash.com", "covers.openlibrary.org"],
  }
};

export default nextConfig;
